
package ;

using scuts.ht.Context;
using scuts.core.Options;
class Benchmark1 {

	

	public static function main () 
	{




	}

	#if single 
	public static function s0 () 
	{
		[1].flatMap_(function (x) return [x]);
	}
	#end

	#if heavy
	public static function h0 () 
	{
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
	}

	public static function h1 () 
	{
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
	}

	public static function h2 () 
	{
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
	}

	public static function h3 () 
	{
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
	}

	public static function h4 () 
	{
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
	}

	

	public static function h5 () 
	{
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
		[1].flatMap_(function (x) return [x]);
	}
	#end
	#if heavy2
	public static function t0 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t1 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t2 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t3 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t4 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t5 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}

	public static function t10 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t11 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t12 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t13 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t14 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t15 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}

	public static function t20 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t21 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t22 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t23 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t24 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	public static function t25 () 
	{
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
		[Some(1)].optionT().flatMap_(function (x) return [Some(x)].optionT());
	}
	#end

}